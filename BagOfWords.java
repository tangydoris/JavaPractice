/*
Bag-of-words model. Write the process of search based on inverted index.
The follow up is given some attributes (an array), how to filter the search result.
 */

import java.util.*;

public class BagofWords {
    // Array is not ordered in any way
    public static final String array[] = {"this is it", "it is this", "this is an apple", "red apple", "orange orange"};

    public static invertedSearch(String array[]) {

    }
}